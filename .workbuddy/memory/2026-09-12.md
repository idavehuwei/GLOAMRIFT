# 2026-09-12

## 烬下 shadow-depths-godot：0.7 打击感与特效层（已提交 c660ff9）
- 新增 `scripts/vfx_3d.gd`：程序化特效池（扩散环/挥砍弧/地面残留/命中光核/冰锥碎石/竖直光柱/蓄能环 + 5 盏动态点光池），由 `world_3d.sync` 驱动；MAX_ITEMS=96 滚动回收，additive 材质 fade 同时压 RGB 与 alpha 保证可淡出。
- `scripts/particles_3d.gd` 重写：新增 burst/ember/frost/dust/snow/motes 分支 + `trail()`（local_coords=false 形成投射物世界拖尾）+ `attach()`（挂到会移动的技能节点上）。
- `world_3d.gd`：`aura_mat()` 加色材质缓存；`impact/explode/cast_charge/swing_arc` API；`sync_combat_fx` 按 kind 生成专属特效（danger 增加填充进度盘与闪烁预警环）；投射物拖尾+火球点光；相机震动改为双频衰减并带 v_offset/roll；敌人受击挤压回弹 + knock_dir 后退；`attach_hero_fx` 加法杖 CastOrb 与剑刃 BladeGlow。
- `model_motion.gd`：`strike(phase)` 三段攻击曲线（WINDUP=0.30 / CONTACT=0.50，蓄力 -0.40 → 击出 1 → 收招 0），替代对称 sin；弓箭手 drawing 由 phase<CONTACT 决定（箭在撒放后隐藏，修了 motion_validation 断言）；受击含胸后仰、倒地先跪后沉、闪避前倾收腿。
- `game.gd`：新增 `cast`（驱动施法宝珠）与 `hitstop`（命中卡帧，dt*=0.16）；`burst()` 升级为完整命中反馈。
- `combat.gd`：命中写 `knock_dir`、暴击 75ms 卡帧、玩家受击 60ms；各职业技能加蓄能/挥砍/枪口特效；陨星与危险区落点改用 `explode()`。
- 模型：球体细分 14×7→18×9、圆柱 12→16 段、圆锥 8→12 段；rig 新增踝关节（行走脚掌滚动）。
- `tools/make_textures.py` v6：法线改由独立高度场计算（原为颜色亮度推法线），反照率烘入 AO，凹陷处粗糙度更高；已重新生成 66 张 256px 贴图。
- 踩坑：Godot 4 无 `CircleMesh`（用自建扇形 ArrayMesh）；`get_meta(key, default)` 缺 key 会报错，需先 `has_meta`；`ball()/block()` 返回节点的 scale 承载尺寸，改辉光大小必须乘 base 而不是覆盖。
- 验证：smoke 1629 / activities 15 / bosses_elites 120 / classes_towns 109 / live_combat 2100 帧 / motion_validation 277 全部 PASS；render_3d 90 帧 754ms（约 8.4ms/帧），draw calls ≈1316。
- 新增 `tests/_fx_preview.gd`：一键渲染各类特效到 `tests/_fx_*.png` 供人工核对。

## 0.8 小宠物系统（下午）
- 新增 `scripts/pets.gd` 子系统：抽卡（宠物商 · 绒绒，第 6 位城镇居民，位置 555,405）、每章 boss 掉落稀有起步伙伴、10 物种（近战/远程，`Data.PETS`）、自动索敌作战、挡刀机制（soak 60% 近战 + splash 30% 范围伤害）、阵亡 30s 自动复活、重复物种转进阶 +2 级、满栏时 make_room 弱者让位。
- 渲染：`world_3d.companion(form,tint)` 程序化构建 10 种造型，`motion.rig(root,4)` 复用四足动画（走路/摆尾/张颚/死亡），翅膀由 `model_motion` 里 `has_meta("wings")` 后置直接驱动（绕过关节插值回中）；复活进度环 `pet_ring`，阵亡倒地 + 环收缩。
- UI：`P` 键 / 角色面板 / 商人对话 → `draw_pet_panel()`（出战卡 + 10 笼位 + 抽卡按钮）；左侧伙伴 HUD 卡；宠物头顶血条与复活倒数。
- 存档 version 3：`pets.snapshot()/restore()`，旧档 [1,2] 兼容（无 pets 字段时为空）。
- 新测试 `tests/pets.gd` 101 checks 全 PASS；全量回归（smoke 1629/activities 15/bosses 120/towns 119/live_combat 2100/motion 280/detail 15/render_3d 36）PASS。
- 踩坑：**Edit 工具对含中文的长 old_string 偶发静默不落盘**（工具报成功但文件未变），连续发生 5 次；改用 `python3 - <<EOF` 读改写并立即 grep 验证才可靠。本轮多个丢掉的编辑：boss_reward 调用、sync_pet 调用、rebuild 清理、pets 对话第 6 条、HUD 卡片——全是带中文锚点的编辑。以后对 game.gd 这类文件做含中文的大段替换，直接用 python 补丁。

## 0.9 成就与收藏系统（上午）
- 新增 `scripts/achievements.gd` 子系统（挂 `game.achv`）+ `content.gd` 的 `ACHIEVEMENTS`（34 项，5 类：战斗/探索/财富/伙伴/旅程，含 2 隐藏）、`ACH_KINDS`/`ACH_KIND_COLORS`、`ARMORY_CAP=30`、`static bestiary_entries()`（35 条 = 5 章 × 7 kind，kind 3 为 boss）。
- 统计键：kills/elites/bosses/max_hit/clean_maps/chests/marks/max_depth/max_chapter/gold_total/legendaries/upgrades/contracts/pets/pet_species/pet_level/playtime/max_level/deaths/cycle/victories/classes。上报点：game.defeat→killed、拾取→looted/mark、开箱→chest、combat.hit_enemy→hit、hurt_player/近战→map_no_hit=false、next_map→map_done(clean)、choose_class→class_picked、activities.upgrade/contract→upgraded/contract_done、pets.grant/gain_xp→pet_granted。gold_total 由 `update()` 里对比 last_gold 自动累加。
- 收藏：bestiary{key:{kills,depth,name}}、armory（rarity≥3，同名取最高 power，sort 后 slice 到 CAP）、trophies{chapter:{count,depth,name}}、pet_seen{species:maxLevel}。
- UI：`G` 键 / 角色面板按钮 / 左侧 HUD 卡（28,443）→ `draw_achv_panel()`（成就 tab：类别列 + 2×4 卡片 + 翻页；收藏 tab：图鉴/陈列/首领/伙伴 4 分组）；右上角 `draw_toasts()` 成就解锁提示。
- 存档 version 4（加 `achv` 字段，兼容 1/2/3）。新测试 `tests/achievements.gd` 270 checks PASS；预览 `tests/_achv_ui_preview.gd`。
- 全量回归：smoke 1629 / activities 15 / bosses 120 / towns 119 / live_combat 2100 / pets 89 / achievements 270 / motion 280 / detail 15 / render_3d 36（953ms，draw calls 1726）。
- 踩坑：① smoke 里手工构造的敌人字典没有 `name` 键，achievements.killed 直接 `e.name` 会报 Invalid access → 一律用 `e.get("name","亡者")`。② **git 仓库根是 /Users/weihu/AI/ShadowDepths 而不是子目录**，`git stash push -u` 会波及上层（当时误 stash 了 1050 个上层删除项，虽已 pop 恢复但极其危险）；对比基线要限定 pathspec（`git stash push -m x -- shadow-depths-godot/scripts`）。③ 测试里直接调 `g._draw()` 会报 "Drawing is only allowed inside _draw()"，应 `queue_redraw()` + `await process_frame`。

## 0.10 开始界面与存档槽（上午）
- 标题画面：`modal="title"` 成为启动默认（原来是 "class"），`draw_title_screen()` 全屏绘制（余烬粒子用 title_time 参数化，无状态）；三张存档槽卡片 `draw_slot_card()` 复用于标题与游戏内「存档管理」（modal="saves"，宽窄两种布局）。
- 存档槽系统：`SLOT_COUNT=3`、`slot_dir`（测试可重定向）、`slot_path(i)`、`use_slot/sync_slot_from_path`、`refresh_slots/slot_info/slot_meta`、`latest_slot/first_empty_slot`、`delete_slot`（二次确认 del_confirm）、`new_game(i)`（reset_run 重建 activities/pets/achv 子系统）、`continue_slot(i)`、`migrate_legacy_save()`（旧单槽 embers_save.json → 槽位一）。save_path 仍是唯一写入目标，测试改 save_path 的写法不受影响。
- 存档 version 5：新增 `meta`（level/class/chapter/stage/cycle/depth/gold/kills/playtime/location/town/saved/achv）；**kills 用 `achv.value("kills")`（累计）而不是 `g.kills`（每图清零）**；`save_game()` 里 FileAccess 写完必须 `f.close()` 再 refresh_slots（否则同帧读回是空文件，JSON 报错）；refresh_slots 对空/损坏文件按 empty 处理。
- 时间格式化：Godot 4.7 的 `Time.get_datetime_dict_from_unix_time(unix)` 只有 1 参（UTC），本地时间要加 `Time.get_time_zone_from_system().bias`（分钟，UTC+8=480）×60。
- 输入：title/saves 走 class/trainer 同一条鼠标分支；Enter 在标题=continue 最近档；F5 快存、F9 存档管理；Esc 篝火菜单新增「存档管理 / 返回标题」。
- 新测试 `tests/saves.gd` 49 checks PASS；`tests/_title_preview.gd` 出 5 张界面截图；classes_towns 断言改为 title；achievements 存档断言 v5+meta（271 checks）。
- 踩坑：① 「杀」字在 SystemFont 回退下渲染成豆腐块（bestiary 早就有此问题），UI 文案统一改「讨伐」；② Bash 工具的命令在沙箱被拦后会以提权方式**重复执行一遍**，非幂等的 python 补丁（assert count==1）第二遍会报 AssertionError 但第一遍已成功——看到 AssertionError 先查文件实际状态。
