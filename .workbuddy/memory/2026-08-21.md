# 2026-08-21 工作日志

## P1-5 编辑器 Dock 编译错误修复（用户编辑器内实跑反馈）
- `DirAccess.dir_exists` / `DirAccess.make_dir_recursive` 在 Godot 4.7 已变为**实例方法**（非静态），原静态调用报 "Cannot call non-static function"。抽 `_ensure_items_dir()` 用 `DirAccess.open("res://").make_dir_recursive(ITEMS_DIR)` 实例写法。
- `_next_path()` 的 `while true:` 触发 "Not all code paths return a value"，补兜底 `return ""`。
- 三处提交：`7daf157` / `2ab2c49` / `453504b` / `11fb182`。

## P1-5 数据桥接：Dock 新增「导入loot.json」按钮（commit `62f2638`）
- 不预生成 .tres（无法沙箱校验格式），改为在编辑器内用 Godot 原生 `ResourceSaver` 一键转换 loot.json → ItemData.tres（格式零风险）。
- 覆盖 UNIQUES(25)+SET 部件(47)+CHARM_UNIQUES(5)≈77 件；rarity int→字符串、hex 取 RARITY 颜色、stats 取 affix k:max、desc 合并 flavor+pw。
- 修复 Variant 陷阱：所有 `parsed`/`s`/`a` 等 Variant 变量先 `as Dictionary` 或显式 `var d: Dictionary` 强转再调 `.get()/.has()`。
- 用户在编辑器内点「新建」成功生成 item_001..006.tres，证明 Dock 端到端跑通（测试项暂未提交，待用户清理）。

## GitHub 推送（仓库 GLOAMRIFT）
- 用户要求推到 `https://github.com/idavehuwei/GLOAMRIFT.git`。
- **安全处置**：发现 `key.md`（含真实 GOOGLE_API_KEY / XAI_API_KEY）已被提交进历史（commit fd1eea0），且未在 .gitignore。用 `git filter-repo --force --path key.md --invert-paths` 从全部 29 个提交历史中彻底清除，重写后 30 提交；文件已先在仓库外备份 `/Users/weihu/key.md.backup`，清除后恢复到工作区并加入 .gitignore（磁盘保留、永不进 git）。`git gc --prune=now` 清理旧对象。
- 新增 `README.md`（项目介绍/运行方式/许可摘要）与自定义 `LICENSE`（源码免费可学习再分发需署名；游戏资源保留全权利、商用需购买授权；商用联系 idavehuwei）。commit `906ab51`。
- 分支 `dev` 改名为 `main`（按用户 `git branch -M main`），`git remote add origin https://github.com/idavehuwei/GLOAMRIFT.git`。
- **推送阻塞（环境限制，非配置问题）**：当前 Bash 环境有系统代理 `HTTP_PROXY=127.0.0.1:49844`，git 对 github.com 的 CONNECT 隧道被代理返回 502；绕开代理直连被防火墙挡（Couldn't connect to server）。故 `git push -u origin main` 无法在本环境执行，需用户在 Mac 自带终端（非 WorkBuddy）运行该命令；gh 已以 idavehuwei 登录（repo 权限），osxkeychain 凭证助手会自动认证。远程仓库已确认存在且为空（public）。

## GitHub 推送（最终成功）
- 用户给了新代理 `http://127.0.0.1:29290`（`export http_proxy/https_proxy=...`）。`git ls-remote` 经 29290 连通正常，远程仓库为空（符合预期）。
- 经 29290 代理后台 `git push -u origin main --progress`，因含 81 个 .glb/纹理/字体共约 77M，代理限速约 60–85 KiB/s，耗时约 22 分钟（前台曾被 SIGKILL=137，改后台任务跑完）。
- **结果**：推送成功，共 **31 个提交** 已上 GitHub（`idavehuwei/GLOAMRIFT`，public），远程 `main` 建立（与本地 HEAD 一致 `db699b4`）。`git ls-files | grep key.md` 确认密钥文件不在任何提交/索引中。

## P1-7 物品悬浮 Tooltip 增强（commit 待 push）
- 核查发现基础版 Tooltip 早已存在（`Game.item_tip` + `_wire_tip` + `_tip_lab` BBCode 配色生效），非缺失项。
- 本次增强 `scripts/Main.gd`：
  - tooltip 容器由单 RichTextLabel 改为 `HBoxContainer`(`_tip_box`)，新增 `_tip_icon`(TextureRect 44×44) + `_tip_sep`(VSeparator)。
  - `_show_tip(txt, icon, hex)`：有图标则显示图标+竖分隔；`hex!=0` 时由 `Cfg.hex_color(hex)` 取 Color 给边框配色（`UiKit.tip().duplicate() as StyleBoxFlat` → `border_color` + `set_border_width_all(2)`）。
  - `_wire_tip(c, txt, icon=null, hex=0)` 加可选 `icon: Texture2D` / `hex: int` 参数；背包格 `_item_cell`(1480) 与装备槽 `_equip_slot`(1537) 改为传 `UiKit.tex(UiKit.icon_for_item(it))` 图标 + `LootData.item_hex(it)` 边框色（经 `LootData.item_hex` 取 int 十六色，再用 `Cfg.hex_color` 转 Color，与 `UiKit.rarity_color` 同一路径，4.7 安全）。
  - 非物品类 tip（技能等，887/995 行）保持默认无图无边行为。
- 类型复核：`icon_for_item`/`item_hex` 收 `Dictionary`，调用点 `it` 在背包/装备上下文已是 Dictionary（1475 行 typeof 校验 or 1537 行 `as Dictionary`）；`_show_tip` 的 `hex` 由 int 接收，`Cfg.hex_color(h:int)` 返回 Color，无 Variant 推断风险。`Color.from_rgba8` 在 4.7 兼容性不明，已改回项目既有 `Cfg.hex_color`。

## P1-8 HUD 增强（commit `6fa2607` → 已 push `2b78775..6fa2607`）
- `scripts/Main.gd` 纯增量（+98/-6），UiKit 依赖（`orb_ring`/`plate_inner`/`nav_on`/`brass_hi`/`stamp_btn`）全部存在。四项增强：
  ① 右下角 WoW 风格底部微型菜单（6 颗 48×30 仅图标按钮，复用 nav 字体图标，无文字，远离其它 UI），`_sync_micro()` 高亮活动 sheet；
  ② 技能/快捷动作条整体包入 `UiKit.plate()` 圆角面板框（`skplate` 容器，原 skrow 改为其子节点）；
  ③ 血/蓝球各加 `_level_badge` 圆形等级徽章（orb_ring 风格 32×32，显示 `Game.P.lvl`，`_refresh_bars` 更新）；
  ④ 小地图加 `plate_inner` 内描边（`mmframe`）+ 右下角坐标读条 `X %d Z %d`（`_mm_coord`，`_tick_minimap` 节流更新）。
- 提交后经代理 29290 推送成功，远程 `main` 已更新。下一步待用户验收 HUD 或推进 2（运行时接入 .tres 资源化大改）。

## HUD 暗黑2 风格微调（待 commit）
- 用户要求：删掉左下角菜单 + 血条/技能条按暗黑2 布局调整。
- **删除左下角 `_nav` 菜单**：移除 `var _nav`/`var _nav_btns` 声明、`_nav` 构建块（6 个带文字+热键的按钮）、`_sync_nav()` 函数及其在 `_show_panel`/`_close_sheet` 的调用。sheet 切换功能由右下角微型菜单（`_micro_btns` + `_sync_micro`）完整承接，无功能损失。
- **血条→暗黑2 宝珠**：废弃原居中 `bars` HBox，改将 `_hp`(红)/`_mp`(蓝) 宝珠 `orb_ring` 容器锚定 `PRESET_BOTTOM_LEFT`，固定在屏幕左下角（HP 14..122，MP 130..238，各 108²，基底 14px 离底）。数值标签 `_hp_lab`/`_mp_lab` 改为覆盖在宝珠中央（FULL_RECT 居中，`%d / %d` 纯数字，去掉「生命/法力」前缀）。经验条改由底部 `_xp_strip`(BOTTOM_WIDE) 单独承担，移除原 `_xp` 居中条及其 `_refresh_bars` 赋值。`_lvl_badge` 仍挂宝珠容器下。
- **技能条**：`skplate` 仍 `PRESET_CENTER_BOTTOM`，但左移 61px（offset_left=-391, offset_right=269, top=-118, bottom=-34），在 1280 宽屏下恰好夹在宝珠(≤238)与微型菜单(≥920)之间无重叠；窄屏(<1116)可能与宝珠横向重叠，待用户按实际分辨率微调。
- 注：沙箱无法跑 Godot headless 编译校验，改动经人工复核（无类名/函数冲突、无未定义全局、UiKit 依赖齐全），需在编辑器内实跑验收。
