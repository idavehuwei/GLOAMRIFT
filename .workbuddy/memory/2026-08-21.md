# 2026-08-21

## 结构调整:移除 HTML/web 版本,仅保留 Godot 工程
- 提交 `69b63f8`(分支 dev):删除 web 游戏版本 `index.html / shadow-depths.html / load.html / create.html / css/ / js/ / public/ / scripts/ / tests/` 及 `node_modules/`(506 文件变更,10704 删除)。
- 清理 `.gitignore`:移除 web 条目(node_modules/、public/...、scripts/...),仅保留 Godot 忽略规则(.godot/、.import/、export/)。
- 目录结构现为:`godot/`(Godot 4.7 工程根,含 project.godot)+ `docs/`(设计文档)+ `key.md`(含 API 密钥,保留未改)+ 工具目录(.claude/、.serena/、.workbuddy/)。
- 同时并入当时 Godot 进度:Sfx 音效自动加载修改、新增 `godot/audio/` 音效、一批 `prop_*.tscn` 场景与新怪物纹理。
- 注意:`godot/models/prop_dummy\n.tscn` 文件名带换行符(既有未追踪文件),已一并提交,后续可重命名修复。
- `key.md` 已在历史中(含 API 密钥),本次未改动也未重新提交其内容。

## 优化与扩展:GitHub 实战参考落地(本地提交,未 push)
- 参考来源:Godot 官方 WorkerThreadPool 文档样例、godot-performance-optimization skill、godot-object-pool / cms-pm/zort 对象池设计。
- 新增 `godot/autoload/ThreadPool.gd`(autoload 单例):封装 WorkerThreadPool,`submit(action, on_done)` 后台算 + call_deferred 回主线程;另有 `submit_group / is_done / wait` 等。
- 新增 `godot/scripts/ObjectPool.gd`(class_name ObjectPool,RefCounted):通用对象池,预分配复用,obtain/release,用于投射物/特效等高频创建场景(当前为就绪工具,待调用方接入)。
- `godot/autoload/WorldState.gd`:新增 `find_path_async(...)`,在 WorkerThreadPool 后台线程跑 A*(find_path 只读 walk()/Cfg 纯数据,不碰场景树),完成后回调;同步 find_path 保持不变,零回归。
- `godot/autoload/Assets.gd`:`_swing` 将 ArmR/ArmL/LegR/LegL 四肢节点缓存到 `n` 的 meta(首帧 find_child 一次,之后复用),消除每帧对每角色 4 次递归 find_child 的热点。
- `godot/project.godot`:注册 ThreadPool 自动加载;渲染段新增 `occlusion_culling/enabled=true` 与 `threads/thread_model=1`(多线程渲染/遮挡剔除)。
- 注意:本沙箱无显示环境,Godot headless 卡在渲染初始化无法跑语法校验,改为严格人工复核;改动均为新增/局部,未改动既有调用方行为。

## GitHub/Google 调研:可借鉴的 Godot RPG 实战(用于 UI/系统优化参考)
- 参考项目:godot-open-rpg(2D回合,RPG结构参考)、Source of Mana(2D MMORPG,F键UI栏+通知框+任务追踪)、godot-tiny-mmo(SQLite持久化+职业)、snakeshu/inventory-system(jame581三层:ItemData Resource/Inventory节点/UI解耦+拖拽)、P0nni(编辑器Dock造物品+原子槽交换)、stewlab(热键栏+合成+分类)、mediwind(塞尔达式动态背包+装备槽+无限滚动)、DialogueQuest(.dqd对话DSL,写手/程序分离)、advanced-quest-system(组合式任务步骤)、HUD最佳实践(CanvasLayer固定HUD+信号驱动+EventBus+平滑血条+respawn bind/unbind)。
- 当前工程差距:Main.gd 2647行巨文件承载全部UI;HUD疑似轮询P字典(非信号驱动);对话TalkData.gd代码硬编码静态答句;物品为dict无.tres资源/编辑器工具/堆叠配置/拖拽UI;无通知系统/任务追踪HUD;单字重字体(NotoSansSC-Regular)。
- 建议借鉴优先级:P0=信号驱动HUD+EventBus、通知/Toast系统、动态背包UI(装备槽+拖拽+分类);P1=数据驱动对话(解耦代码)+分支选项UI、Resource物品+编辑器Dock、任务追踪HUD+步骤式任务;P2=拆分Main.gd为场景化UI组件、SQLite存档(可选)。

## P0 三项落地(本地提交,未 push)
- 新增 `godot/autoload/EventBus.gd`(autoload 单例):全局信号总线,含 stat_changed/leveled_up/gold_changed/bag_changed/quest_changed/target_changed/buff_changed 与反馈层 notify/achievement 信号。核心解耦 UI 与各系统。
- 新增 `godot/autoload/Notify.gd`(autoload,CanvasLayer):右上角通知栈,统一承载 升级/拾取/成就/系统 提示,带淡入淡出与最多 5 条自动回收;kind 配色(info/good/warn/loot/ach);`Notify.toast()` 便捷入口走 `EventBus.notify`。
- 新增 `godot/scripts/InvCell.gd`(class_name InvCell extends Button):可拖拽物品格,点击=原行为(装备/鉴定/卸下),拖拽=背包↔装备↔护符移动(`_get_drag_data/_can_drop_data/_drop_data` + 预览),落点由 `Main._inv_drop` 协调(复用 Game.equip_from_bag/unequip_slot/charm_to_bag)。
- `godot/scripts/Main.gd`:
  - `_refresh_bars(dt)` 改为**平滑条**(显示值 lerp 向真实值收敛,消除硬跳变);并钩子升级/拾取经 EventBus→Notify 通知。
  - `_item_cell` / `_equip_slot` 改为返回 `InvCell`;`_item_cell` 增加 qty>1 堆叠角标。
  - `_bag_grid` 增加**分类筛选标签**(全部/武器/护甲/符文/护符/材料),按 `_bag_filter` 过滤。
  - 新增 `_cat` / `_inv_drop` / `_swap_bag` 协调函数;`InvCell.host=self` 在 `_ready` 设置;成就 toast 同时 `EventBus.notify`。
- `godot/project.godot`:注册 EventBus 与 Notify 自动加载。
- 注:沙箱仍无法跑 Godot 语法校验(渲染初始化挂起),已严格人工复核;改动均为新增/局部,既有点按/右键行为保留,零回归。P0-1 的"信号驱动"在现有 Game.ui_refresh 信号驱动基础上补了 EventBus 解耦层 + 平滑条。
