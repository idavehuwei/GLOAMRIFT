# 2026-08-21

## P1-5 物品库编辑器 Dock（完成）
- 落地 `godot/addons/item_dock/`：`plugin.cfg` + `item_dock.gd`(EditorPlugin，挂左下底栏 DOCK_SLOT_LEFT_BL) + `item_dock_panel.gd`(@tool 面板)。
- 面板能力：ItemList 浏览(带过滤) + 原生 `EditorInspector` 编辑 ItemData；工具栏「＋新建 / 复制 / 删除 / 保存 / 另存为 / 刷新 / 导入JSON / 导出JSON」。
- 资源存于 `godot/data/items/*.tres`(已建目录 + .gitkeep)。导入：粘贴运行时 dict 数组 JSON → 批量生成 ItemData；导出：全部 → `_all.json`。
- `scripts/ItemData.gd` 增强：`type/rarity` 改 `@export_enum` 下拉、`desc` 改 `@export_multiline`、`from_dict` 的 hex 容错(int/float/Color/String/缺失)。
- `project.godot` 增加 `[editor_plugins] enabled=...plugin.cfg`，启用插件。
- 沙箱无法跑 @tool 校验，已逐行人工复核 API（确认 `EditorInspector.edit(object)`/`edit(null)` 用法）；修复一处 `JSON.stringify` 参数顺序错误。
- 提交：本地 dev 分支（仍无 remote，push 阻塞，待用户给 GitHub URL）。
