# 2026-08-23 工作日志

## GLB 优化与 4.7.1 兼容收尾（续 08-22）

### 诊断结论（本轮只读工具完成，Bash 本轮回传异常不可用）
- **mob_treant 加载失败根因**：`models/src/mob_treant.glb.import` 第 7 行 `valid=false` 且无 `path=` 缓存指针；对比 `mob_golem.glb.import` 有 `path=res://.godot/imported/mob_golem.glb-*.scn`（有效）。Godot 把 treant 判为导入失败态故 `load()` 跳过。需重跑 Godot 导入使其重新生成有效配置并验证 meshes/armatures/anims。若仍失败则从 `/tmp/glb_opt_backup/mob_treant.glb` 用 Blender 同管线重导出。
- **字体缺失（启动 ERROR）**：`project.godot:49` 引用 `res://fonts/NotoSansSC-Regular.ttf`，且导入日志还引用 `NotoSerifSC-Black.ttf`；项目内 Glob `**/*.ttf` 全空——两字体文件彻底丢失，启动即 `Error loading custom project font`。待恢复文件，或临时移除 `theme/custom_font` 消除报错。
- **item_dock 插件崩溃（已修）**：`addons/item_dock/item_dock_panel.gd:283` `import_dialog.accepted.connect(...)`——Godot 4 中 `AcceptDialog` 信号已更名 `confirmed`，导致插件 `_enter_tree` 时 SCRIPT ERROR。已改为 `confirmed`（全项目仅此一处 `.accepted`，无其他同类）。

### 已落地修复
- item_dock `accepted` → `confirmed`（代码修复，无需运行验证即可确认是 4→4 信号更名）。

### 待执行（均依赖 Bash：Godot/Blender/git，本轮回传异常暂阻）
- 重导入 treant 清 valid=false（任务 #86）
- 恢复/禁用字体（#87）
- 干净导入+启动校验归零 ERROR（#88）
- 提交 8 优化 GLB + 96 .glb.import EMBED + Main.gd join 修复 + item_dock 修复并 push（#89）
- 剩余 83 GLB/音频体积审计（#90）、4.7 兼容隐患扫描（#91）、运行时性能剖析（#92）、巨型 autoload 拆分（#93）

### GLB 优化进度
- 8 个 >5MB 重型 GLB 已用 Blender 降采样（纹理 4K→1024²、无 Draco、export_skins/yup/animations）优化：102.88MB → 21.69MB（-78.9%）；全项目 91 GLB 现约 72MB。
- 关键教训：Godot 4.7 GLB 导入器**不支持 Draco**；纹理抽取改用 EMBED(`embedded_image_handling=2`) 杜绝外部 PNG 残留；同文件导出覆盖会损坏 GLB，须先导出临时文件再 replace。
