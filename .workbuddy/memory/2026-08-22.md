# 2026-08-22 工作日志

## 掉落/装备系统大扩展（让刷怪更有动力）
commit `36f1bdc` → 已 push 至 origin/main。

- **Data.BASES**：装备基础池 ~25 → ~50 种（武器/各部位新增），roll_item 随机 pick，零风险。
- **AFFIX/FEEL_AFFIX**：词缀池扩充，仅用 apply_mod 已消费的 k（见 MEMORY.md 约束），扩大随机组合。
- **RUNES**：+10 种（兽牙/骨白/圣壁/血怒/风暴/震地/潮涌/橡筋/护壁），加进 Data.RUNES 即被 roll_rune/make_rune_item 无缝消费。
- **loot.json**（tools/inject_loot.py 注入，仅追加、内置 k 消费校验+JSON 合法）：套装 8→11、传奇 25→35、传奇护符 5→8、护符词缀 18→26、护符底 size1=7/2=6/3=5。
- **Data.MOB_DROP**（新增 const）：按怪物 type 定义 itemMul(物品掉率乘子)/force(装备类型倾向)/runes(专属符文)/runeCh(专属符文概率)/charmMul(护符乘子)。
- **Game.gd roll_drop 集成 MOB_DROP**：普通怪物品掉率 20%→32%、护符 9%→14%；有表怪额外按概率掉专属符文（如狼→兽牙、骷髅→骨白、骑士/魔像→圣壁、盗匪→血怒）。
- 人工复核：括号平衡 OK；所有 Variant 来源（`.get()`/`Cfg.pick()`）按约定显式标注类型；k 消费校验通过；JSON 合法。沙箱无法 Godot 实跑，待编辑器验收。

## 章末 Boss 强化（更难 + 能力更丰富，终章极难）
未提交（与上一轮记忆文件一并待 commit）。

- **新增 4 种 Boss 能力**，复用现有原语（`_proj` 投射物 / `add_zone` 伤害区 / `hurt_player` / `knockback_player`）：
  - `volley` 朝玩家扇形散射 7 发（橘色）；`beam` 精准高伤射线（红，spd19 dmg×1.35）；`curse` 玩家脚下诅咒领域（hurt="player" 伤害区，持续掉血）；`enrage` 半血狂暴（提速/攻速/伤害，cor 有 enrageStrong 强狂暴）。
- **_boss_skill 重写**：支持 `skills` 数组轮转（按 skIdx 循环），未知技能回退到 nova；每个技能独立冷却，狂暴时 cd×0.7。
- **5 位章末 Boss 配组合技 + 狂暴**（Data.gd ETYPES，均提升 hp/dmg/攻速）：
  - 瓦拉克 ch1 `["slam","nova"]`；无名唱者 ch2 `["nova","summon"]`；格雷 ch3 `["slam","volley"]`；众议之声 ch4 `["nova","curse","summon"]`；**柯尔 ch5 `["slam","volley","beam","curse","summon"]` + enrageStrong（终章极难）**。
- **spawn_enemy 透传**：`skills/skIdx/enrage/enrageAt/enrageStrong/enraged`。
- 人工复核：括号平衡 OK；无 Variant 推断报错；enrage 块在 tick_world 半血触发（仅 boss 且 enrage=true）。
- 风险：沙箱无法 Godot 实跑；柯尔 lvl70 基础 hp 8600（×难度 mult 后约 20万+），dmg 高，需编辑器实测校准难度；若过难/过易再调 ETYPES 数值即可。

