# 2026-08-22 工作日志

## HUD 暗黑2 一行布局重排（godot/scripts/Main.gd）
按用户要求：技能栏一行，左边血球（圆）、右边法力/能量球（圆）。
- HP 红宝珠：`PRESET_BOTTOM_LEFT`，距底 14、x 14–122（保持不变）。
- MP 蓝宝珠：从「紧邻 HP 右侧（左下）」改为 `PRESET_BOTTOM_RIGHT`，x (W-238)–(W-130)、距底 14、同 108²，与 HP 分居底部两端。
- 技能条 skplate：`PRESET_CENTER_BOTTOM`，由偏左的 `-391/269` 改回对称 `-330/330`，居中夹在双球之间，与宝珠同处底部一行。
- 微型菜单 micro：原在 `PRESET_BOTTOM_RIGHT` 距底 12–44（与 MP 宝珠 14–122 重叠）→ 上移到距底 126–158，位于 MP 宝珠正上方，纵向分离不重叠。
- 校验：1280 宽屏下 HP(≤122) / 技能条(310–970) / MP(≥1042) / 微型菜单(920–1268 但 y 在 126+，与技能条 y34–118 分离) 互不重叠；窄屏(≈1024) 技能条右端会与 MP 球临界重叠，已提示用户如用更窄分辨率再调。
- 沙箱无法 Godot headless 编译校验，改动经人工复核（无类名/函数冲突、UiKit 依赖齐全），待用户在编辑器实跑验收。

## NPC 面板「对话/交易」分离重构（godot/scripts/Main.gd）
用户诉求：优化角色/技能/对话/铁匠面板；核心「对话是对话，交易是交易」。
- 新增 `_npc_mode` 变量（"talk"/"trade"），`open_npc(id, mode="talk")` 支持模式参数。
- 铁匠卡登：原 `_open_kaden`（对话台词+7 交易 tab 混排）拆成 `_kaden_talk`（问候+打听+「进入铁匠铺」按钮）与 `_kaden_trade`（纯 7-tab 交易+「← 返回对话」按钮）。所有交易回调刷新用 `mode="trade"`。
- 赌徒沃恩：原 `open_npc` 内 70 行 vaun 段（对话+骰子+商店混排）抽成 `_vaun_talk`（问候+打听+「坐庄」按钮）与 `_vaun_trade`（纯骰子+商店+「← 返回对话」）。`_tick_die` 赌局结束刷新改 `open_npc("vaun","trade")`。
- 关键坑：NPCDEF 里 kaden/vaun **无 kind 字段**，原会先走通用对话分支（def.kind==""）再走 match 交易段 = 双重混排（即用户吐槽的根因）。修复：通用对话分支条件排除 `id != "kaden" and id != "vaun"`（未改 Data.gd，因 WorldState 把 NPC kind 存 W.npcs 并用于交互判定，加 kind 会有副作用，stash 即靠 kind 区分）。
- match 分派：kaden/vaun 按 `_npc_mode` 切 talk/trade；mara/bridge/spring 等保持原状不动。
- 角色/技能面板（open_char/open_skills）本已是独立清晰面板，作为样板未改；本次重点是把混乱的 NPC 交互重构成同等清晰度。
- 沙箱无法编译，改动经人工复核（函数定义齐、无残留 `open_npc("kaden"/"vaun")` 字面、结构完整），待编辑器验收对话↔交易切换。

## NPC 对话/交易分离（第二批：mara / selin）
用户「其他npc也继续分离」。延续同一框架（_npc_mode + talk/trade 双视图）。
- **药剂商玛拉**：原 `_open_mara`（问候+回想+补给商店挤同一视图）重命名 `_mara_trade`（纯商店，去问候、加「← 返回对话」），新增 `_mara_talk`（问候+打听+「逛铺子（药水·补给）」按钮）。内部 6 处 `open_npc("mara")` 刷新全部改 `open_npc("mara","trade")`。
- **执政官塞琳**：原 match 内「对话(通用分支)+任务交付」混排拆成 `_selin_talk`（greet+打听+「查看委托」按钮）与 `_selin_quest`（纯任务列表+交付/「← 返回对话」）。交付回调 `open_npc("selin")` 改 `open_npc("selin","trade")`。
- 通用对话分支排除名单追加 `id != "selin" and id != "mara"`（防双重渲染对话，与 kaden/vaun 同手法，不动 Data.gd/kind）。
- match 分派：selin/mara 按 `_npc_mode` 切 talk/trade（与 kaden/vaun 并列）。
- 未动：stash/board/waystone/spring/bridge/caravan/apprentice 等单功能面板（无对话+交易混排），以及 harun/wick/quill/nock 等纯对话 guard（only 限定场景、无交易）。
- 沙箱无法编译，人工复核（4 个新函数定义齐、无 `_open_mara` 残留、无裸 `open_npc("mara")`、match 结构完整、_npc_mode/_talk_pick 声明齐全），待编辑器验收。

## 角色面板重排为暗黑手游风格（godot/scripts/Main.gd）
用户贴图示意：顶部等级/战力/货币，左右装备槽列夹中央人物模型，底部四栏属性网格。
- `open_char()` 不再复用 `_open_gear()`，改为独立构建新布局：`open_bag()` 继续走 `_open_gear()` 保持旧布局（纸娃娃+统计+背包）。
- 新增 `_char_header()`：三卡片顶栏——左（职业图标+名字+Lv）、中（装等）、右（金币 🪙），均用 `UiKit.gcard()` 深色卡片底。
- 新增 `_char_stats_grid()`：底部四栏（概况/属性/攻击/防御）横向网格，使用 `UiKit.stat_row` 统一行样式；概况含生命/法力/经验/装等，属性保留力量/敏捷/体魄/精神+未分配点（pts>0 时仍出 +str/dex/vit/ene 按钮），攻击/防御复用 `Game.p_stats()`。
- 新增 `_char_sets_row()`：套装/残句/遗物紧凑摘要放最底部，保留 `Game.rebuild_powers()`。
- 装备槽分布：左列 weapon/gloves/belt/ring1/offhand，右列 helm/armor/boots/ring2/amulet（10 槽各 5），中央 `_doll_vp` 模型预览（260×320 min，保持比例）。
- 交互：`open_char()` 触发 `_rebuild_doll()`，`_tick_doll` 已覆盖 `_sheet == "char"` 渲染；装备槽用现有 `_equip_slot()`，点击卸下并 `_reload_gear()`。
- 沙箱无法编译，人工复核（函数定义齐、无 `_open_gear()` 残留于 open_char、UiKit 依赖 `gcard/stat_row/group` 均存在、`Game.p_stats` 字段对齐），待编辑器实跑验收布局/字号/间距。
