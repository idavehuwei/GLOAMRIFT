# 2026-08-22 工作日志

## 掉落/装备系统大扩展（让刷怪更有动力）
commit `36f1bdc` → 已 push 至 origin/main。

- **Data.BASES**：装备基础池 ~25 → ~50 种（武器/各部位新增），roll_item 随机 pick，零风险。
- **AFFIX/FEEL_AFFIX**：词缀池扩充，仅用 apply_mod 已消费的 k（见 MEMORY.md 约束），扩大随机组合。
- **RUNES**：+10 种（兽牙/骨白/圣壁/血怒/风暴/震地/潮涌/橡筋/护壁），加进 Data.RUNES 即被 roll_rune/make_rune_item 无缝消费。
- **loot.json**（tools/inject_loot.py 注入，仅追加、内置 k 消费校验+JSON 合法）：套装 8→11、传奇 25→35、传奇护符 5→8、护符词缀 18→26、护符底 size1=7/2=6/3=5。
- **Data.MOB_DROP**（新增 const）：按怪物 type 定义 itemMul(物品掉率乘子)/force(装备类型倾向)/runes(专属符文)/runeCh(专属符文概率)/charmMul(护符乘子)。
- **Game.gd roll_drop 集成 MOB_DROP**：普通怪物品掉率 20%→32%、护符 9%→14%；有表怪额外按概率掉专属符文（如狼→兽牙、骷髅→骨白、骑士/魔像→圣壁、盗匪→血怒）。
- 人工复核：括号平衡 OK；所有 Variant 来源（`.get()`/`Cfg.pick()`）按约定显式标注类型；k 消费校验通过；JSON 合法。沙箱无法 Godot 实跑，待编辑器验收。

## 章末 Boss 强化（更难 + 能力更丰富，终章极难）
未提交（与上一轮记忆文件一并待 commit）。

- **新增 4 种 Boss 能力**，复用现有原语（`_proj` 投射物 / `add_zone` 伤害区 / `hurt_player` / `knockback_player`）：
  - `volley` 朝玩家扇形散射 7 发（橘色）；`beam` 精准高伤射线（红，spd19 dmg×1.35）；`curse` 玩家脚下诅咒领域（hurt="player" 伤害区，持续掉血）；`enrage` 半血狂暴（提速/攻速/伤害，cor 有 enrageStrong 强狂暴）。
- **_boss_skill 重写**：支持 `skills` 数组轮转（按 skIdx 循环），未知技能回退到 nova；每个技能独立冷却，狂暴时 cd×0.7。
- **5 位章末 Boss 配组合技 + 狂暴**（Data.gd ETYPES，均提升 hp/dmg/攻速）：
  - 瓦拉克 ch1 `["slam","nova"]`；无名唱者 ch2 `["nova","summon"]`；格雷 ch3 `["slam","volley"]`；众议之声 ch4 `["nova","curse","summon"]`；**柯尔 ch5 `["slam","volley","beam","curse","summon"]` + enrageStrong（终章极难）**。
- **spawn_enemy 透传**：`skills/skIdx/enrage/enrageAt/enrageStrong/enraged`。
- 人工复核：括号平衡 OK；无 Variant 推断报错；enrage 块在 tick_world 半血触发（仅 boss 且 enrage=true）。
- 风险：沙箱无法 Godot 实跑；柯尔 lvl70 基础 hp 8600（×难度 mult 后约 20万+），dmg 高，需编辑器实测校准难度；若过难/过易再调 ETYPES 数值即可。

## 掉落平衡微调（高品质更稀有、整体更低、饰品/戒指最低）
commit `c4d0420` → 已 push 至 origin/main。

- **需求**：高品质装备掉落概率改小；整体掉落概率改小；有些怪专门掉落保留；饰品/戒指掉落要更低。
- **roll_item 稀有度曲线压低**（仅非强制 else 分支）：否则白62/魔26/稀9/史2.5/传0.5（原白46/魔32/稀16.5/史4.5/传1）。`force_rare`（boss/elite/su/hoard）路径未动，仍稳定掉高品质。
- **装备类型加权随机**：原 9 类型均匀 pick → 改为权重池（普通部位×3、amulet/ring×1），饰品/戒指占比约 22%→9%。MOB_DROP 的 `force` 仍可让特定怪定向掉饰品（如 wraith→amulet/ring、imp/ashmage→ring）。
- **普通怪物品掉率 0.32→0.20**（roll_drop 末位 else）；精英/boss/hoard/su 不受影响。MOB_DROP 专属掉落（itemMul+专属符文 runeCh）保留。
- 校验：括号平衡 OK；force_rare 完好；0.32 无残留。沙箱无法 Godot 实跑，待编辑器验收掉率手感。

## GLB 资产核查（缺口与冗余）
待提交（与美化改动同批）。

- **当前 81 个 GLB**（godot/models/src/，全在 src 子目录）。
- **3 个怪物类型无模型映射 → 刷出即空节点（最严重）**：`golem`(灰烬峡谷/桥墩井道)、`treant`(暮语林地)、`wraith`(沉降区/桥墩井道/多个副本) 在野外与副本 mobs 中实际刷出，但 `Data.ENEMY_ASSET` 无条目；`Assets.make_actor` 对缺文件 id 最终 `return Node3D.new()`（完全不可见，比方块更糟）。需补 `mob_golem`/`mob_treant`/`mob_wraith` GLB 并加映射。
- **5 个孤儿怪物 GLB（冗余/旧命名）**：`mob_ghoul`(ghoul 已改映射 mob_zombie)、`mob_lord`(lord 已改 boss_warrok)、`mob_maw`、`mob_skelrogue`、`mob_slime` —— 全工程无引用。**→ 已清理（见下方「孤儿 GLB 清理」节）**。
- **武器 GLB 完全缺失**：无 sword/axe/bow/staff 等独立武器模型；角色模型不含可换装武器，库存/掉落装备无 3D 表现（仅 prop_weaponstand 装饰）。属已知缺口。
- **缩略图/立绘/头像缺失**：无 godot/thumbnails、无 portrait/avatar/thumbnail 资源；与记忆「缺武器、音效、预览缩略图」一致。
- **NPC 10 个全部接线**：npc_bridge/harun/kaden/mara/nock/quill/rogue/selin/vaun/wick 均被 Data.gd NPC 定义 asset= 引用；prop_* 道具全部被引用无孤儿。
- 低质线索：prop_rock1~5(1.5~3KB)/prop_column(7.4KB)/prop_carpet(8.9KB) 体积极小，疑为占位低模，可后续替换高质岩石/柱体。

## hy-3d 批量补 3 怪物 + 2 建筑（进行中）
任务：用 hy-3d 生成 3 个缺模型怪物（golem/treant/wraith，含骨骼+动画）+ 顺带 2 个建筑（world_tower 守望塔 / world_shrine 祭坛）凑满每日 5/5 额度，一起做。

- **根因确认**：golem/treant/wraith 的 `Data.ETYPES` 早已存在（Data.gd 472/475/486），缺的只是 `ENEMY_ASSET` 映射 + GLB/tscn。已补 `ENEMY_ASSET`（`"golem":"mob_golem","treant":"mob_treant","wraith":"mob_wraith"`）到 `godot/autoload/Data.gd` 与源头 `godot/tools/make_data.py`（未重跑 make_data.py，避免覆盖此前手动编辑；仅同步该块）。
- **tscn 已建**：`mob_golem/trreant/wraith.tscn` + `world_tower/world_shrine.tscn`，均 instance `res://models/src/<id>.glb`。
- **生成管线（已验证）**：
  - `tools/gen_hy3d.py`：自写编排器（替代 skill 的 batch 脚本），LowPoly+model3.0+face_count22000，2 并发 / 5 每日 / 3600s 预算 / manifest 可续。token 经 `connect_cloud_service` 取 tempToken 管道传入。
  - `tools/rig_monster.py`：Blender 5.2 headless 对怪物做 nearest-bone 刚性蒙皮（13 骨通用双足骨架，按 bbox 比例摆放）+ 4 个 NLA 剪辑 `idle/run/attack/die`，导出 `skins+yup+NLA_TRACKS`。已对 `mob_skeleton.glb` 冒烟通过（产出 skins+4 anims）。
  - `tools/finalize_assets.sh` + `tools/validate_glb.py`：生成完成后一键 rig 三怪物 + 拷贝两建筑 + 校验（怪物需 skins≥1 且含 idle/run/attack/die）。
- **动画契约（Assets.tick_anim）**：怪物按 `e.anim.walk/atk` 自动选 idle/run(或 walk)/attack(或 slash/punch)，`_clip` 大小写+别名匹配；die 当前游戏不显式播放（死亡靠 dieT 计时移除），但保留 die 剪辑以备。
- 状态：hy-3d 后台生成中（RtkIsm，~40min），完成后跑 finalize → validate → commit。
- 建筑为静态（无动画）；接法仅 tscn 包装，未并入 make_models.py 生成流程（后续可按需接线到世界布置）。

## 孤儿 GLB 清理（已完成）
- 5 个孤儿 + 被替换的 `mob_zombie` 已处理：
  - **接（1）**：`mob_ghoul` 高清（32 骨/51 节点）优于现用 `mob_zombie`（13 骨/25 节点），将 `ghoul` 类型映射由 `mob_zombie` 改为 `mob_ghoul`，同步 `Data.gd:518` 与 `make_data.py:483`；删除 `mob_zombie.glb`+`.tscn`（避免产生新孤儿）。`ghoul` 用于 crypt/nest/sinkf/tide 4 副本，属高频怪，升级有价值。
  - **删（4）**：`mob_lord`(lord 已是 Boss 类型 boss_warrok，非 Boss 的 mob_lord 无家且撞名)、`mob_maw`(无 maw 怪物类型)、`mob_skelrogue`(无 skelrogue 类型)、`mob_slime`(无 slime 类型且只含 idle 一个剪辑，残缺) —— 删除各自 `.glb`+`.tscn`。5 个文件均无源码引用（仅旧 web 导出包 `export/web/index.pck` 二进制残留，已 gitignore，不影响运行）。
- 校验：删除后无 godot/ 源码残留引用；`mob_ghoul` 确认 `skins=1` + 动画 `attack/die/idle/run`（符合 Assets.tick_anim 契约）。改动尚未 commit（与美化/掉落批待一并提交，或单独提交——待用户确认）。

## 武器 GLB 体系（角色手持 + 掉落表现）【已完成实现+静态校验，未提交】
补齐「GLB 资产核查」里标注的武器缺口——此前角色模型不含可换装武器、库存/掉落装备无 3D 表现。

- **10 把武器 GLB（程序化生成，不走 hy-3d 额度）**：`tools/gen_weapons.py`（Blender 5.2 headless）生成 `wpn_sword/axe/mace/spear/dagger/staff/bow/crossbow/book/wand`，低多边形 PBR（金属 blade + 木/骨 grip），握把在几何原点、沿 +Y 向上（yup 导出），单网格 2~4 材质。每把一份 `godot/models/wpn_*.tscn`（instance 对应 `src/wpn_*.glb`）。
- **Blender 5.2 坑（已踩并记录）**：`primitive_uv_sphere_add` 已去 `rings` 参数→改用 `primitive_ico_sphere_add(subdivisions)`；Principled BSDF 节点按类型查找非按名；Emission 改名 `Emission Color`/`Emission Strength`；`export_scene.gltf` 的 `export_materials`/`export_yup`/`export_skins` 等部分布尔改枚举（`export_materials='EXPORT'`），稳妥起见导出只传 `filepath/export_format='GLB'/export_yup=True/export_materials='EXPORT'`。
- **Data.gd 武器映射（同步 make_data.py）**：`WPN_SHAPES`(形状→wpn id)、`WPN_NAME_SHAPE`(中文武器名→形状，含去品质前缀+子串匹配，避免「磨损的橡木杖」错配成剑)、`WPN_WKIND_POOL`(怪物 wkind→同系形状池，增加持武变化)、`WPN_GRIP`(各形状握把微调 pos/rot/scl，便于编辑器微调)；辅助 `wpn_id()/wpn_shape_for_name()/wpn_pool_for_wkind()`。
- **Assets.gd `equip_weapon(actor, glb_id, tint_hex=-1, shape="")`**：递归找 Skeleton3D → 按 `handslot.r/hand.r/RightHand/...` 优先级找手骨骼 → BoneAttachment3D 挂武器 tscn → 应用 WPN_GRIP 变换；支持换装（meta `wpn_attach` 先 queue_free 旧的）；`tint_hex>=0` 染自发光（注：GLB 网格走 surface material、无 material_override，tint 对武器实际为 no-op，怪物/玩家武器保持原生 PBR 观感，掉落的高稀有度仍由 OmniLight3D 表现）。
- **接入点**：
  - 怪物 `spawn_enemy`：有 wkind 则按池随机挂武器并染 `t.weapon` 色（野兽无 wkind 不挂）；Boss 走 RightHand 骨骼同样生效。
  - 玩家 `_spawn_player_view` 后调 `_refresh_player_weapon(node)`；`Game.equip_from_bag`(weapon) 与 `unequip_slot`(weapon) 同步重挂/移除。
  - 掉落 `_loot_mesh`：weapon 类物品直接显示真实武器 GLB（scale 0.6 + 旋转漂旋），高稀有度加 OmniLight3D 发光。
- **手骨骼实证**：char_warrior/mage/archer 与常规怪均为 `handslot.r/.l` + `hand.r/.l`；boss_warrok 为 Mixamo 命名 `RightHand/LeftHand` 等——`_hand_bone` 优先级已覆盖。
- **校验**：10 GLB 全部合法（meshes=1, mats 2~4）；Data/Assets/WorldState/Game 括号平衡（剥离字符串后）；make_data.py 可 `py_compile`。沙箱无法 Godot 实跑。
- **待编辑器验收（用户手动）**：握把缩放/朝向（WPN_GRIP.scl 默认 0.6~1.0）需按实际角色比例微调——这是无法 headless 验证的视觉项；怪物武器 tint 为 no-op（原生观感）。
- 武器 GLB 为新增未跟踪文件；与美化/掉落/孤儿清理批同在 Working Tree，待用户确认分组后提交。

## hy-3d 收尾完成 + 全功能分批提交（本日收官）
- **hy-3d 后台生成早已 DONE**：`tools/hy3d_out/` 含 5 GLB + 预览图 + manifest（RtkIsm 约 40min 后静默完成，此前误判为未完）。
- **最终收尾（17:5x 完成）**：5 GLB 拷入 `godot/models/src/`（mob_golem/treant/wraith/world_tower/world_shrine）；3 怪物跑 `rig_monster.py`（Blender 5.2 headless）加 13 骨通用骨架 + idle/run/attack/die 4 动画；2 建筑跑 `ground_buildings.py` 把底座落到 y=0。
- **接地约定确认**：怪物网格以原点居中（脚在原点下方，参考 mob_ghoul y=[-1,1]）；地表物体（prop/建筑）底座 y=0（prop_rock1 实测 y=[-0.05,0.70]）。rig_monster 不改 y，保持居中，与现有怪物一致。
- **校验全绿**：`tools/validate_glb.py` → 3 怪物 skins=1 + 4 动画齐全；2 建筑 meshes=1；8 个 prop 接地 y≈0 且尺寸合理（岩石 H 0.45~0.75、立柱 1.37/2.85、地毯平贴 0.05）。
- **分批提交（约 7 组）**：data(武器常量+怪物映射+掉落表+ghoul) / 武器体系 / 怪物+建筑 / 孤儿清理 / prop 替换 / 视觉收尾 / 工作记忆。`hy3d_out/`(68MB) 与 `__pycache__/` 已加 .gitignore 不提交。
- **仍待编辑器手动验收（无法 headless）**：① 武器握把缩放/朝向 WPN_GRIP.scl 0.6~1.0 按实际角色比例微调；② 怪物武器 tint 为 no-op（走原生 PBR）；③ 各怪物动画在游戏内播放手感；④ 掉落武器 GLB 漂旋表现。

