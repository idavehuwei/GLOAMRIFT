# 2026-08-22 工作日志

## HUD 暗黑2 一行布局重排（godot/scripts/Main.gd）
按用户要求：技能栏一行，左边血球（圆）、右边法力/能量球（圆）。
- HP 红宝珠：`PRESET_BOTTOM_LEFT`，距底 14、x 14–122（保持不变）。
- MP 蓝宝珠：从「紧邻 HP 右侧（左下）」改为 `PRESET_BOTTOM_RIGHT`，x (W-238)–(W-130)、距底 14、同 108²，与 HP 分居底部两端。
- 技能条 skplate：`PRESET_CENTER_BOTTOM`，由偏左的 `-391/269` 改回对称 `-330/330`，居中夹在双球之间，与宝珠同处底部一行。
- 微型菜单 micro：原在 `PRESET_BOTTOM_RIGHT` 距底 12–44（与 MP 宝珠 14–122 重叠）→ 上移到距底 126–158，位于 MP 宝珠正上方，纵向分离不重叠。
- 校验：1280 宽屏下 HP(≤122) / 技能条(310–970) / MP(≥1042) / 微型菜单(920–1268 但 y 在 126+，与技能条 y34–118 分离) 互不重叠；窄屏(≈1024) 技能条右端会与 MP 球临界重叠，已提示用户如用更窄分辨率再调。
- 沙箱无法 Godot headless 编译校验，改动经人工复核（无类名/函数冲突、UiKit 依赖齐全），待用户在编辑器实跑验收。

## NPC 面板「对话/交易」分离重构（godot/scripts/Main.gd）
用户诉求：优化角色/技能/对话/铁匠面板；核心「对话是对话，交易是交易」。
- 新增 `_npc_mode` 变量（"talk"/"trade"），`open_npc(id, mode="talk")` 支持模式参数。
- 铁匠卡登：原 `_open_kaden`（对话台词+7 交易 tab 混排）拆成 `_kaden_talk`（问候+打听+「进入铁匠铺」按钮）与 `_kaden_trade`（纯 7-tab 交易+「← 返回对话」按钮）。所有交易回调刷新用 `mode="trade"`。
- 赌徒沃恩：原 `open_npc` 内 70 行 vaun 段（对话+骰子+商店混排）抽成 `_vaun_talk`（问候+打听+「坐庄」按钮）与 `_vaun_trade`（纯骰子+商店+「← 返回对话」）。`_tick_die` 赌局结束刷新改 `open_npc("vaun","trade")`。
- 关键坑：NPCDEF 里 kaden/vaun **无 kind 字段**，原会先走通用对话分支（def.kind==""）再走 match 交易段 = 双重混排（即用户吐槽的根因）。修复：通用对话分支条件排除 `id != "kaden" and id != "vaun"`（未改 Data.gd，因 WorldState 把 NPC kind 存 W.npcs 并用于交互判定，加 kind 会有副作用，stash 即靠 kind 区分）。
- match 分派：kaden/vaun 按 `_npc_mode` 切 talk/trade；mara/bridge/spring 等保持原状不动。
- 角色/技能面板（open_char/open_skills）本已是独立清晰面板，作为样板未改；本次重点是把混乱的 NPC 交互重构成同等清晰度。
- 沙箱无法编译，改动经人工复核（函数定义齐、无残留 `open_npc("kaden"/"vaun")` 字面、结构完整），待编辑器验收对话↔交易切换。

## NPC 对话/交易分离（第二批：mara / selin）
用户「其他npc也继续分离」。延续同一框架（_npc_mode + talk/trade 双视图）。
- **药剂商玛拉**：原 `_open_mara`（问候+回想+补给商店挤同一视图）重命名 `_mara_trade`（纯商店，去问候、加「← 返回对话」），新增 `_mara_talk`（问候+打听+「逛铺子（药水·补给）」按钮）。内部 6 处 `open_npc("mara")` 刷新全部改 `open_npc("mara","trade")`。
- **执政官塞琳**：原 match 内「对话(通用分支)+任务交付」混排拆成 `_selin_talk`（greet+打听+「查看委托」按钮）与 `_selin_quest`（纯任务列表+交付/「← 返回对话」）。交付回调 `open_npc("selin")` 改 `open_npc("selin","trade")`。
- 通用对话分支排除名单追加 `id != "selin" and id != "mara"`（防双重渲染对话，与 kaden/vaun 同手法，不动 Data.gd/kind）。
- match 分派：selin/mara 按 `_npc_mode` 切 talk/trade（与 kaden/vaun 并列）。
- 未动：stash/board/waystone/spring/bridge/caravan/apprentice 等单功能面板（无对话+交易混排），以及 harun/wick/quill/nock 等纯对话 guard（only 限定场景、无交易）。
- 沙箱无法编译，人工复核（4 个新函数定义齐、无 `_open_mara` 残留、无裸 `open_npc("mara")`、match 结构完整、_npc_mode/_talk_pick 声明齐全），待编辑器验收。

## 角色面板重排为暗黑手游风格（godot/scripts/Main.gd）
用户贴图示意：顶部等级/战力/货币，左右装备槽列夹中央人物模型，底部四栏属性网格。
- `open_char()` 不再复用 `_open_gear()`，改为独立构建新布局：`open_bag()` 继续走 `_open_gear()` 保持旧布局（纸娃娃+统计+背包）。
- 新增 `_char_header()`：三卡片顶栏——左（职业图标+名字+Lv）、中（装等）、右（金币 🪙），均用 `UiKit.gcard()` 深色卡片底。
- 新增 `_char_stats_grid()`：底部四栏（概况/属性/攻击/防御）横向网格，使用 `UiKit.stat_row` 统一行样式；概况含生命/法力/经验/装等，属性保留力量/敏捷/体魄/精神+未分配点（pts>0 时仍出 +str/dex/vit/ene 按钮），攻击/防御复用 `Game.p_stats()`。
- 新增 `_char_sets_row()`：套装/残句/遗物紧凑摘要放最底部，保留 `Game.rebuild_powers()`。
- 装备槽分布：左列 weapon/gloves/belt/ring1/offhand，右列 helm/armor/boots/ring2/amulet（10 槽各 5），中央 `_doll_vp` 模型预览（260×320 min，保持比例）。
- 交互：`open_char()` 触发 `_rebuild_doll()`，`_tick_doll` 已覆盖 `_sheet == "char"` 渲染；装备槽用现有 `_equip_slot()`，点击卸下并 `_reload_gear()`。
- 沙箱无法编译，人工复核（函数定义齐、无 `_open_gear()` 残留于 open_char、UiKit 依赖 `gcard/stat_row/group` 均存在、`Game.p_stats` 字段对齐），待编辑器实跑验收布局/字号/间距。

## 四面板统一为卡片/网格风格（godot/scripts/Main.gd）
用户先二选一确认范围：选「统一 技能/天赋/任务/成就 四个面板为角色面板同款卡片/网格风格」。
- 新增通用辅助 `_info_card(title, value, col=UiKit.gold())`：返回 `UiKit.gcard()` 深色卡片，左标题(ash 11px)+右值(18px col)，与 `_char_header` 卡片视觉一致。插在 `open_char` 之前。
- **技能面板 open_skills**：加 `_info_card("可用技能点", skPts)` 顶栏；右侧详情包入 `gcard` PanelContainer（detail_card min 320），列表项保持 cell/gcard 选中态；学习/符文/栏位绑定(_pick_skill_id/_assign_skill_slot)逻辑全保留。
- **天赋面板 open_talents**：加 `_info_card` 双卡（可用点/已点）；新增 `_talent_card(t)` 把每个天赋包成 gcard 卡片（图标+名+描述+已点亮/点亮按钮），左右两列网格；点亮/洗点逻辑保留。
- **任务面板 open_quests**：加 `_info_card("追踪中", 当前追踪任务名)` 顶栏；列表项按 done/选中态上 cell 绿/铜/gcard 样式；右侧详情包入 gcard 卡片；追踪/取消追踪逻辑保留。
- **成就面板 open_achs**：加 `_info_card` 双卡（已解锁 X/Y、完成度%）；每个分类包成 gcard 卡片、选中项铜色高亮、done 绿；右侧详情包入 gcard 卡片（◆图标/描述/进度条/金币奖励）。进度条变量原 `bar` 改名 `bar2` 避免与顶栏 `bar` 冲突。
- 实现手法：Python 脚本按 `func` 边界（下一个 `\nfunc `）精确整段替换四个函数 + 插入两个辅助，避免手工长段匹配出错；括号/花括号平衡校验通过。
- 沙箱无法 Godot headless 编译，改动经人工复核（四个函数定义各一次、_info_card/_talent_card 就位、UiKit 依赖 gcard/cell/cell_hover/slot/icon_rect/icon_for_glyph/icon_for_skill/group/stat_row/brass_hi/ash/dim/gold 均存在、Game.* 调用沿用既有），待编辑器实跑验收卡片视觉。
- 注：行囊面板(open_bag→_open_gear)仍保留旧布局，用户当时未选此项；若后续要「左右分栏+中央模型」同角色面板，可再单独改造。

## 行囊面板重排为同角色面板风格（godot/scripts/Main.gd）
用户「把行囊面板也改成同风格」。
- `open_bag()` 不再复用旧 `_open_gear()`，改为与 `open_char()` 完全对齐的内联布局：顶部 `_char_header()` 信息条（姓名/等级·装等·金币）→ 主体 HBox（左装备槽列 weapon/gloves/belt/ring1/offhand | 中央 `_doll_vp` 模型预览 260×320 | 右装备槽列 helm/armor/boots/ring2/amulet）→ 操作提示 → `_charm_grid`（遗物袋）→ `_bag_grid`（行囊，含分类筛选 tab）→ 底部 foot（金币/容量）。
- 装备槽、模型旋转交互、卸下/鉴定/丢弃回调全部沿用 `_equip_slot`/`_on_doll_input`/`_bag_grid`，功能零损失。
- 清理被取代的旧死代码（仅 `_open_gear` 使用）：删除 `func _open_gear` / `func _paper_doll` / `func _stat_panel` / `func _slot_pad` / `func _pick_stat_tab` 及 `var _stat_tab`。用 Python 脚本按顶层 `func` 边界精确定位删除，校验无残留引用、依赖(_char_header/_equip_slot/_charm_grid/_bag_grid/_rebuild_doll/_on_doll_input/_doll_vp)齐备。
- 至此角色/行囊/技能/天赋/任务/成就 + 全部 NPC 面板均统一为暗黑风卡片/网格语言。沙箱无法 Godot headless 编译，改动经人工复核，待编辑器实跑验收视觉。

## 面板视觉质感升级 Tier-1（godot/scripts/UiKit.gd + Main.gd）
用户「面板还是不好看，从 google/github 找找例子」。先调研：GitHub 标杆 don-tnowe/godot-wyvernbox-inventory（动作RPG库存，Abaddon哥特字体+凹槽插槽）、alfredbaudisch/GodotDynamicInventorySystem（响应式+分类卡片）；Godot 官方 Theme 体系共识=90% 统一主题/StyleBoxFlat，质感靠「双层描边+阴影+圆角+凹陷标题」。
集中升级共享视觉语言 UiKit（一次性抬升所有面板，零逻辑风险）：
- `plate()`：边框由 0.42 铜色 1px 加粗为 `brass()` 2px + 半径 4 + 阴影 24/offset(0,6) + expand_margin 2 → 金边外框+立体投影。
- 新增 `plate_inner()` 增强（brass_hi alpha 0.5 内嵌描边）；Main.gd 在 `pad` 与 `_panel_body` 之间插入 `frame_inner`(PanelContainer+plate_inner) → **双层金边外框**。
- `gcard()`：边框 0.165→0.42 铜色 + 阴影 8 → 卡片凸起成铭牌。
- 新增 `recess()`；`header()` 改为「凹陷标题牌」（recess 背景牌 + 黄铜字 + 1px 描边 + 两侧 _orn 饰条）。
- `stat_row()`：外包 PanelContainer + `_stat_row_bg()`(半透明凹槽底纹，shadow_size=0) → 法典条目感、更易读。
- `slot()`：填充由 0.086 加深为 0.035 + brass 环边 + 内阴影 → 凹槽插槽质感。
- 字体：项目仅 NotoSansSC-Regular 无粗体，标题靠加大字号/黄铜色/凹陷牌/描边出效果，未引入新字体文件。
- 沙箱无法 Godot headless 编译，改动经人工复核（括号平衡、新函数齐、无残留），待编辑器实跑验收。Tier-2 备选：9-slice 石质纹理外框、哥特中文字体、面板四角铆钉、装备槽圆形深凹。

## 面板视觉 Tier-2（全做：石质外框+暗黑中文字体+圆形插槽+四角铆钉）
用户「1,2,3 都优化」。新增资源：`godot/textures/frame.png`(256² 石质外框,9-slice margin=36,PIL程序化生成:石质+黄铜内描边+四角铆钉+噪点)、`godot/textures/socket.png`(128² 圆形深凹插槽,径向渐变+黄铜环)、`godot/fonts/NotoSerifSC-Black.ttf`(思源宋体变量字体 25MB,OFL,经代理 29290 从 github/google-fonts 下载)。
UiKit 新增：`frame_plate()`(StyleBoxTexture 载入 frame.png,set_margin_all(36))、`serif_font()`(load 思源宋体,FontFile.has_method 守卫下调 set_variation_coordinates({"wght":900}) 取 Black 重字重)、`socket_style()`(StyleBoxTexture 载入 socket.png)。`header()` 标题牌标签改用 serif_font()+描边。
Main.gd：主面板 `_panel` 由 `plate()` 改 `frame_plate()`(小控件 minimap/toast/pplate/skplate 仍用扁平 `plate()` 不被重纹理压垮)；`_panel_title` 与 `_char_header` 的姓名/Lv/装等 改用 serif_font()；`_equip_slot` 填满/空槽均改用 `socket_style()`(圆形深凹插槽,InvCell extends Button 的 normal stylebox 即背景,已验证生效)；新增 `_studs:Array`,`_panel` 四角加 4 颗黄铜铆钉(ColorRect,锚定面板四角),并在 `_show_panel`/`_close_sheet` 同步显隐(避免面板关闭后铆钉残留)。
- 风险/注意：思源宋体为 25MB 变量字体,提交后仓库增重;若渲染想更"古典"可换 Noto Serif SC 或更重的静态字重。沙箱无法 Godot headless 编译,改动经人工复核(括号平衡、新函数/资源齐、set_variation_coordinates 已 has_method 守卫防编译/运行崩溃),待编辑器实跑验收石质外框/字体/插槽铆钉实际观感。

## 面板视觉 Tier-3：字体加大、石框收窄、NPC/商店/Boss 血条统一石框语言
用户「字体大一点、石框窄一些，继续统一 NPC 面板（铁匠/赌徒/杂货商）或 Boss 血条、商店列表也套这套石框语言」。
- 字体：`UiKit.header()` 标题牌 16→18；`Main.gd` 主面板标题 `_panel_title` 18→22；`_char_header` 姓名 16→18、等级 18→20、装等 20→22、金币图标 18→20、金币值 15→16。
- 石框收窄：`tools/gen_textures.py` 新增并纳入仓库（PIL 生成），重绘 `frame.png` 使 stone/brass 边框内缩到约 24px（四角铆钉同步缩小）；`UiKit.frame_plate()` 9-slice margin 由 36→24，面板外框视觉厚度明显下降。
- 统一 NPC/商店卡片语言：`UiKit.gcard_hi()` 新增（高亮版 gcard：黄铜高亮边框+加深底+更大投影）；`Main.gd` 新增 `_topic_btn(text, cb, active)` 与改写 `_btn()` 使所有 NPC 面板按钮使用 gcard/gcard_hi 风格；`_sitem()`（商店物品格）背景由 cell 改 gcard，图标 28→30、高度 52→54。
  - 覆盖范围：通用 NPC 对话打听/分支按钮、塞琳/卡登/沃恩/玛拉四 NPC 的话题与分支、铁匠 7 个交易 tab、赌徒赌注/押大小按钮、所有「返回对话」「关掉」「进入铁匠铺」「坐庄」等动作按钮、银行/悬赏板等 `_btn` 调用处。
- Boss 血条：`Main.gd` 新增 `_boss_bar`（顶部中央石框 PanelContainer，`frame_plate`）、`_boss_name`（宋体 18px）、`_boss_hp`（暗红填充进度条）。在 `_refresh_combat_hud()` 中检测 `Game.cine_boss` 或当前目标 `boss=true`，隐藏普通 `_target` 标签，显示石框 Boss 血条并实时同步 hp/hpMax。
- 新增 `tools/gen_textures.py` 方便后续再次调整外框/插槽纹理。
- 风险/注意：`_btn` 全局改为 gcard 后，stash/board/waystone/spring/bridge 等面板按钮也会卡片化，与 NPC 统一，属预期效果；如个别面板嫌厚重可后续单独恢复 cell。沙箱无法 Godot headless 编译，改动经人工复核（括号平衡 0 错误、新函数/资源齐、Boss 血条变量均声明），待编辑器实跑验收字号/石框宽度/NPC 卡片/Boss 血条。
